Lesson1vocab = createObject(“roArray”, 10, false)
Lesson2vocab = createObject(“roArray”, 10, false)
Lesson3vocab = createObject(“roArray”, 10, false)
Lesson4vocab = createObject(“roArray”, 10, false)
"add the items for these arrays using  Push.___"
sub MultipleChoiceLogic(list, correctlist)
"some sort of if statement using the lesson nodes to decide the vocab"
chosen_vocab = Int(Rnd()*10)
chosen_vocab = random_number
for n = 1 to 3 step 1
  while random_number = chosen_vocab
    random_number = Int(Rnd()*10)
  end while
end for
